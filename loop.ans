- hosts: all
  remote_user: ec2-user
  become: yes
  tasks:
      - name: Install Package
        ansible.builtin.yum:
           name: "{{ item }}"
           state: present
        loop:
           - mysql
           - php
           - unzip
           - ipl
